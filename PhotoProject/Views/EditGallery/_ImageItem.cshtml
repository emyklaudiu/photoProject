@model PhotoProject.Models.ImageModel[]

@for(int i=0; i<Model.Count();i++)
{
    Html.Label(Model[i].imageURL);
    Html.Label("blabla");
}
@Html.Label(Model[0].imageURL)
<img src="@Model[0].imageURL" />